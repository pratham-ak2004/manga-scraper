package ui

type ButtonVariant string

const (
	ButtonDefault     ButtonVariant = "default"
	ButtonDestructive ButtonVariant = "destructive"
	ButtonOutline     ButtonVariant = "outline"
	ButtonSecondary   ButtonVariant = "secondary"
	ButtonGhost       ButtonVariant = "ghost"
	ButtonLink        ButtonVariant = "link"
)

type ButtonSize string

const (
	ButtonSizeDefault ButtonSize = "default"
	ButtonSizeSm      ButtonSize = "sm"
	ButtonSizeLg      ButtonSize = "lg"
	ButtonSizeIcon    ButtonSize = "icon"
	ButtonSizeIconSm  ButtonSize = "icon-sm"
	ButtonSizeIconLg  ButtonSize = "icon-lg"
)

type ButtonProps struct {
	Text      string
	Type      string        // "button", "submit", "reset"
	Variant   ButtonVariant
	Size      ButtonSize
	Disabled  bool
	Class     string // Additional custom classes
	Icon      templ.Component
	HxPost    string
	HxGet     string
	HxTarget  string
	HxSwap    string
	HxTrigger string
}

templ Button(props ButtonProps) {
	<button
		type={ getButtonType(props.Type) }
		class={ getButtonClasses(props) }
		data-variant={ string(props.Variant) }
		data-size={ string(props.Size) }
		if props.Disabled {
			disabled
		}
		if props.HxPost != "" {
			hx-post={ props.HxPost }
		}
		if props.HxGet != "" {
			hx-get={ props.HxGet }
		}
		if props.HxTarget != "" {
			hx-target={ props.HxTarget }
		}
		if props.HxSwap != "" {
			hx-swap={ props.HxSwap }
		}
		if props.HxTrigger != "" {
			hx-trigger={ props.HxTrigger }
		}
	>
		if props.Icon != nil {
			<span class="inline-flex items-center justify-center gap-2">
				@props.Icon
				if props.Text != "" {
					<span>{ props.Text }</span>
				}
			</span>
		} else {
			{ props.Text }
		}
        { children... }
	</button>
}

func getButtonType(btnType string) string {
	if btnType == "" {
		return "button"
	}
	return btnType
}

func getButtonClasses(props ButtonProps) string {
	// Base classes matching shadcn
	baseClasses := "inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-lg text-sm font-medium transition-all disabled:pointer-events-none disabled:opacity-50 outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2"
	
	// Variant classes
	variant := props.Variant
	if variant == "" {
		variant = ButtonDefault
	}
	
	variantClasses := ""
	switch variant {
	case ButtonDefault:
		variantClasses = "bg-primary text-primary-foreground shadow hover:bg-primary/90"
	case ButtonDestructive:
		variantClasses = "bg-destructive text-destructive-foreground shadow-sm hover:bg-destructive/90"
	case ButtonOutline:
		variantClasses = "border border-input bg-background shadow-sm hover:bg-accent hover:text-accent-foreground"
	case ButtonSecondary:
		variantClasses = "bg-secondary text-secondary-foreground shadow-sm hover:bg-secondary/80"
	case ButtonGhost:
		variantClasses = "hover:bg-accent hover:text-accent-foreground"
	case ButtonLink:
		variantClasses = "text-primary underline-offset-4 hover:underline"
	}
	
	// Size classes
	size := props.Size
	if size == "" {
		size = ButtonSizeDefault
	}
	
	sizeClasses := ""
	switch size {
	case ButtonSizeDefault:
		sizeClasses = "h-10 px-4 py-2"
	case ButtonSizeSm:
		sizeClasses = "h-9 rounded-md gap-1.5 px-3 text-xs"
	case ButtonSizeLg:
		sizeClasses = "h-11 rounded-md px-8"
	case ButtonSizeIcon:
		sizeClasses = "size-10"
	case ButtonSizeIconSm:
		sizeClasses = "size-9"
	case ButtonSizeIconLg:
		sizeClasses = "size-11"
	}
	
	// Combine all classes
	classes := baseClasses + " " + variantClasses + " " + sizeClasses
	
	if props.Class != "" {
		classes += " " + props.Class
	}
	
	return classes
}

